# 圖表與資料
pandas>=2.2
plotly>=6.7

# 本地 Dash 版（app.py）；GitHub Actions 的更新流程不需要這兩個
dash>=4.1
dash-bootstrap-components>=2.0

# TrendForce 爬蟲（scraper.py）
curl_cffi>=0.15
beautifulsoup4>=4.13

# 選用：只有重新產生網站圖示（make_icons.py）時才需要
# pillow>=10
