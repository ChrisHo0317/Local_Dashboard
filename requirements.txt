# 圖表與資料
pandas>=2.2
plotly>=6.7

# 本地 Dash 版（app.py）。GitHub Actions 其實用不到，但排程直接安裝整份
# requirements.txt，避免再發生「加了套件卻忘了同步 workflow」的問題。
dash>=4.1
dash-bootstrap-components>=2.0

# 積分榜的中文名簡繁混雜，用 OpenCC 統一成台灣正體（f1_scraper.py）
opencc-python-reimplemented>=0.1.7

# TrendForce 爬蟲（scraper.py）
curl_cffi>=0.15
beautifulsoup4>=4.13

# 選用：只有重新產生網站圖示（make_icons.py）時才需要
# pillow>=10
